# Development toolchain for quill.
# Install with: task setup  (or: brew bundle)

brew "bun"     # runtime, bundler, test runner, package manager
brew "go-task" # task runner (Taskfile.yml)
brew "gh"      # GitHub CLI (PRs, CI, releases)
